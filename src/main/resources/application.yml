spring:
  application:
    name: CineMAX
  datasource:
    url: jdbc:postgresql://localhost:5432/cinemaxdb
    username: postgres
    password: sql-diyari-22
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
